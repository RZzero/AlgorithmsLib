#include <bits/stdc++.h>

using namespace std;

int main(){
  int T;
  cin >> T;
  while(T--){
    int n, m, l;
    cin >> n >> m >> l;
    map<int,int> kills;
    vector<pair<int,int>> pairs;
    for(int i = 0; i < m; i++){
      int x,y; cin >> x >> y;
      pairs.push_back({x,y});
    }
  }
  return 0;
}