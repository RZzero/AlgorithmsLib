#include <bits/stdc++.h>

using namespace std;

struct Event{
  int id, x;

  
};
int main() {
  int N, Q, z, x, c, dum;
  vector<int> numbers;
  cin >> N;

  for(int i = 0; i < N; i++){
    cin >> dum;
    numbers.push
  }
  return 0;
}