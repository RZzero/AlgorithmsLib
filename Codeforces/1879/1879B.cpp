#include <bits/stdc++.h>
using namespace std;
using Long = long long;

void solve() {
  Long N;
  cin >> N;

  vector<int> A(N);
  vector<int> B(N);
  for (auto &a : A)
    cin >> a;
  for (auto &b : B)
    cin >> b;

  // Para el I que estoy iterando debo tomar
}
int main() {
  int T;
  cin >> T;
  while (T--) {
    solve();
  }
}