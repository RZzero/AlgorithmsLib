#include <bits/stdc++.h>
// #include <bits/stdc++.h>

using namespace std;

int main (){
  int N;
  cin >> N;
  vector<int> V(N);
  for(auto v: V) cin >> v;

  int answer = -1;
  for(int i = 0; i < V.size(); i++){
    
  }
  return 0;
}