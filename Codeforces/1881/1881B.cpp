#include <bits/stdc++.h>
using namespace std;
using Long = long long;
void solve() {
  vector<int> A(3);
  for (auto &a : A)
    cin >> a;

  sort(A.begin(), A.end());
}
int main() {
  int T;
  cin >> T;
  while (T--)
    solve();
  return 0;
}