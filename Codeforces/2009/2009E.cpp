#include <bits/stdc++.h>
using namespace std;
using Long = long long;
Long gauss(int N) {
  return (N * (N + 1)) / 2;
}

void solve() {
  Long N, K;
  cin >> N >> K;
}

int main() {
  int T;
  cin >> T;
  while (T--) {
    solve();
  }
  return 0;
}